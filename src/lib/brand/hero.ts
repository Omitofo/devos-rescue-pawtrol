/**
 * Home hero animal collage.
 *
 * Drop the provided collage at: public/brand/hero-animals.jpg
 * (see project artifacts / design handoff if the file is not in the repo yet).
 * Soft gradient behind the image is always present in page.tsx.
 */
export const HERO_ANIMALS_DATA_URL = "/brand/hero-animals.jpg";
